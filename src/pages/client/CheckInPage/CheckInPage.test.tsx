import { describe, it } from "vitest";

describe("CheckInPage", () => {
  it.todo("renders assigned questionnaire questions");
  it.todo("allows selecting a response for each question");
  it.todo("submits responses and shows confirmation");
  it.todo("shows empty state when no questionnaire is assigned");
});
