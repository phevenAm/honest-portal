import { describe, it } from "vitest";

describe("LoginPage", () => {
  it.todo("renders email and password inputs");
  it.todo("shows error when credentials are invalid");
  it.todo("redirects to dashboard on successful login");
  it.todo("disables submit button while loading");
});
