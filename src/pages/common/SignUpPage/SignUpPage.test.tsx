import { describe, it } from "vitest";

describe("SignUpPage", () => {
  it.todo("renders all signup fields including access token");
  it.todo("shows error when access token is invalid");
  it.todo("shows error when access token has already been used");
  it.todo("shows error when passwords do not match");
  it.todo("submits successfully with valid token and credentials");
});
