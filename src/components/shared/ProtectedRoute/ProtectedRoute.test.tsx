import { describe, it } from "vitest";

describe("ProtectedRoute", () => {
  it.todo("renders children when user has the required role");
  it.todo("redirects to /login when user is not authenticated");
  it.todo("redirects admin away from client routes");
  it.todo("redirects client away from admin routes");
});
