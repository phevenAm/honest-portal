import { describe, it } from "vitest";

describe("Modal", () => {
  it.todo("renders children inside modal");
  it.todo("closes when close button is clicked");
  it.todo("closes when backdrop is clicked");
});
